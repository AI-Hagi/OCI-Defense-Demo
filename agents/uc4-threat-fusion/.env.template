# Threat-Fusion-Agent operator env — copy to .env locally, never commit.
# Only the OCID-typed values are sensitive; the human-readable ones are fine.

# -- Always required --------------------------------------------------------
OCI_REGION=eu-frankfurt-1
COMPARTMENT_OCID_DEFENCE_DEMO=ocid1.compartment.oc1..aaaaaaaamcjaobwgnwwwkaphfzuzavq2dez6jkonahdwsn6ys7apqgiqelmq

# -- BLOCKED #1: Cohere dedicated cluster OCID ------------------------------
# Empty until the LARGE_COHERE limit increase lands. With this empty,
# the agent definition cannot be deployed to OCI Agent Factory in a
# procurement-grade configuration.
COHERE_CLUSTER_OCID=

# -- ORDS endpoint base (Tag 6 lives here) ---------------------------------
ORDS_BASE_URL=https://G8CC3767E64A14A-SOVDEF26.adb.eu-frankfurt-1.oraclecloudapps.com/ords

# -- BLOCKED #2: ORDS OAuth2 wiring ----------------------------------------
# All three are unset until 06_ords_oauth.sql lands and the client is
# registered. Until then, the four tools are public POST endpoints —
# never deploy this agent to a non-sandbox environment in that state.
ORDS_OAUTH_TOKEN_URL=
OAUTH_CLIENT_ID_VAULT_OCID=
OAUTH_CLIENT_SECRET_VAULT_OCID=

# -- Per-invocation OLS cap (set by the caller, not at deploy time) --------
# This is the X-OLS-Label-Max header value the agent forwards on every
# tool call. Operator UI sets it based on the requesting user's
# clearance. Allowed values: OFFEN | INTERN | NFD. Setting GEHEIM is
# clamped server-side to NFD (demo cap).
AGENT_OLS_LABEL_MAX=NFD
