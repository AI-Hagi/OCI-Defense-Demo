# Sovereign Defence — FastAPI services .env template
# Copy to services/<name>/.env and fill in real values. NEVER commit the result.

# Oracle 26ai Autonomous Database
ORACLE_USER=admin
ORACLE_PASSWORD=changeme
ORACLE_CONNECT_STRING=sovdef26_tp
TNS_ADMIN=/home/ubuntu/wallet
WALLET_PASSWORD=changeme

# Default tenant used in local dev when no ingress is injecting it
X_TENANT_DEFAULT=T001
