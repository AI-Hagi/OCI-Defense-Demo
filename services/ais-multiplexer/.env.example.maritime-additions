# Maritime additions for the AIS multiplexer (Pattern B, UC4).
#
# Source-of-truth for these variables is the repo-root .env.example, but that
# file does not yet exist (only .oci-cloudguard.env / .oci-devops.env are present
# in the repo root). Until the root .env.example is added, this fragment
# documents the canonical maritime variable block that the AIS multiplexer
# service consumes at runtime.
#
# Once /home/ubuntu/oci-defense-demo/.env.example is created, the block below
# should be appended verbatim at the END of that file (do not interleave with
# existing sections).

# UC4 — Maritime AIS multiplexer (services/ais-multiplexer)
VAULT_AIS_STREAM_KEY_OCID=ocid1.vaultsecret.oc1.eu-frankfurt-1.placeholder  # TODO: aus OCI Vault setzen
AIS_BBOX_DEFAULT=53.0,8.0,56.0,22.0  # south,west,north,east — Ostsee-Default
MULTIPLEXER_PORT=8001
