# Document Intelligence service dependencies.
#
# Required runtime environment variables:
#   ORACLE_USER            DB user (e.g. DICE_APP)
#   ORACLE_PASSWORD        DB user password
#   ORACLE_CONNECT_STRING  TNS alias (default: sovdef26_tp)
#   TNS_ADMIN              Wallet directory (default: /app/wallet)
#   WALLET_PASSWORD        Wallet PKCS#12 password
#   EMBED_MODEL            Optional sentence-transformers name (default: all-MiniLM-L6-v2)
fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
oracledb==2.5.1
python-dotenv==1.0.1
httpx==0.27.2
tenacity==9.0.0
sentence-transformers==3.3.1
numpy==2.1.3
# Required by FastAPI's UploadFile / Form for the /upload multipart endpoint
# (services/doc-intelligence/app/routers/upload.py).
python-multipart==0.0.20
# OCI Generative AI Inference (Cohere R+ OnDemand) for the /chat endpoint —
# the previous MVP-stub answer is replaced by a real synthesised response.
# Auth via OKE Workload Identity signer; degrades gracefully to the stub
# when the SDK or signer is unavailable.
oci>=2.130,<3.0
